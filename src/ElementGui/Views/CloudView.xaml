<UserControl
    x:Class="ElementGui.Views.CloudView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:res="clr-namespace:ElementGui.Resources"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vm="clr-namespace:ElementGui.ViewModels"
    d:DataContext="{d:DesignInstance Type=vm:CloudViewModel}"
    TextElement.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">

    <UserControl.Resources>
        <Style x:Key="SettingsCard" TargetType="Border">
            <Setter Property="Background" Value="#08FFFFFF" />
            <Setter Property="BorderBrush" Value="#12FFFFFF" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
        </Style>
        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="Margin" Value="0,24,0,8" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="#9ca3af" />
        </Style>
        <Style x:Key="RowDivider" TargetType="Border">
            <Setter Property="Height" Value="1" />
            <Setter Property="Background" Value="#10FFFFFF" />
            <Setter Property="Margin" Value="14,0" />
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel MaxWidth="640" Margin="8,16,8,24">
            <TextBlock FontSize="22" FontWeight="Bold" Text="{x:Static res:Strings.Cloud_Title}" />

            <!-- SETUP -->
            <TextBlock Style="{StaticResource SectionHeader}" Text="{x:Static res:Strings.Cloud_Section_Setup}" />
            <Border Style="{StaticResource SettingsCard}">
                <StackPanel>
                    <Grid Margin="14,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock FontSize="13" FontWeight="Medium" Text="{x:Static res:Strings.Cloud_DllStatus}" />
                            <TextBlock Margin="0,4,0,0" FontSize="12" Foreground="{Binding InstallStatusColor}" Text="{Binding InstallStatus}" TextWrapping="Wrap" />
                        </StackPanel>
                        <ui:SymbolIcon Grid.Column="1" Margin="12,0,0,0" Symbol="PlugConnected24" FontSize="24" Foreground="{Binding InstallStatusColor}" />
                    </Grid>
                    <Border Style="{StaticResource RowDivider}" />
                    <StackPanel Margin="14,12" Orientation="Horizontal">
                        <ui:Button Command="{Binding InstallCommand}" Content="{x:Static res:Strings.Cloud_Install}" Icon="{ui:SymbolIcon ArrowDownload24}" Appearance="Primary" Margin="0,0,8,0" IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBool}}" />
                        <ui:Button Command="{Binding UninstallCommand}" Content="{x:Static res:Strings.Cloud_Uninstall}" Icon="{ui:SymbolIcon Delete24}" IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBool}}" />
                    </StackPanel>
                    <Border Background="#0a000000" BorderBrush="#12ffffff" BorderThickness="1" CornerRadius="4" Padding="8" Margin="14,0,14,12" MinHeight="120" MaxHeight="240">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <TextBox Text="{Binding InstallLog, Mode=OneWay}" FontFamily="Cascadia Code,Consolas,Courier New" FontSize="11" Foreground="#6b7280" Background="Transparent" BorderThickness="0" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True" />
                        </ScrollViewer>
                    </Border>
                </StackPanel>
            </Border>

            <!-- GENERAL -->
            <TextBlock Style="{StaticResource SectionHeader}" Text="{x:Static res:Strings.Cloud_Section_General}" />
            <Border Style="{StaticResource SettingsCard}">
                <StackPanel>
                    <Grid Margin="14,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock FontSize="13" FontWeight="Medium" Text="{x:Static res:Strings.Cloud_Enabled}" />
                            <TextBlock Margin="0,4,0,0" FontSize="12" Foreground="#6b7280" Text="{x:Static res:Strings.Cloud_Enabled_Hint}" TextWrapping="Wrap" />
                        </StackPanel>
                        <ui:ToggleSwitch Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center" IsChecked="{Binding IsEnabled, Mode=TwoWay}" />
                    </Grid>
                    <Border Style="{StaticResource RowDivider}" />
                    <Grid Margin="14,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock FontSize="13" FontWeight="Medium" Text="{x:Static res:Strings.Cloud_AutoSync}" />
                            <TextBlock Margin="0,4,0,0" FontSize="12" Foreground="#6b7280" Text="{x:Static res:Strings.Cloud_AutoSync_Hint}" TextWrapping="Wrap" />
                        </StackPanel>
                        <ui:ToggleSwitch Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center" IsChecked="{Binding AutoSync, Mode=TwoWay}" />
                    </Grid>
                </StackPanel>
            </Border>

            <!-- LOCAL FOLDER -->
            <TextBlock Style="{StaticResource SectionHeader}" Text="{x:Static res:Strings.Cloud_Section_LocalFolder}" />
            <Border Style="{StaticResource SettingsCard}">
                <StackPanel>
                    <Grid Margin="14,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock FontSize="13" FontWeight="Medium" Text="{x:Static res:Strings.Cloud_LocalPath}" />
                            <TextBlock Margin="0,4,0,0" FontSize="12" Foreground="#6b7280" Text="{x:Static res:Strings.Cloud_LocalPath_Hint}" TextWrapping="Wrap" />
                        </StackPanel>
                        <Grid Grid.Column="1" Margin="12,0,0,0" MinWidth="260">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <ui:TextBox Grid.Column="0" VerticalAlignment="Center" Text="{Binding LocalPath, Mode=TwoWay}" PlaceholderText="C:\Saves" />
                            <ui:Button Grid.Column="1" Margin="8,0,0,0" Command="{Binding BrowseLocalFolderCommand}" Content="..." Padding="8,4" />
                        </Grid>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- STATUS -->
            <TextBlock Style="{StaticResource SectionHeader}" Text="{x:Static res:Strings.Cloud_Section_Status}" />
            <Border Style="{StaticResource SettingsCard}">
                <StackPanel>
                    <Grid Margin="14,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock FontSize="13" FontWeight="Medium" Text="{x:Static res:Strings.Cloud_Status}" />
                            <TextBlock Margin="0,4,0,0" FontSize="12" Foreground="{Binding ConnectionStatusColor}" Text="{Binding ConnectionStatus}" TextWrapping="Wrap" />
                        </StackPanel>
                        <ui:Button Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center" Command="{Binding CheckConnectionCommand}" Content="{x:Static res:Strings.Cloud_Check}" Icon="{ui:SymbolIcon ArrowSync24}" IsEnabled="{Binding IsChecking, Converter={StaticResource InverseBool}}" />
                    </Grid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
